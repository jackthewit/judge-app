# 심사 웹앱 사용 가이드

> **✅ 배포 완료 (2026-09-09)**
> - 심사위원 접속: https://jackthewit.github.io/judge-app/
> - 관리자: https://jackthewit.github.io/judge-app/?admin=admin-7k2p9x
> - DB: Supabase `judge-app` 프로젝트 (연결 완료) / 코드: github.com/jackthewit/judge-app
> - 코드 수정 시: 이 폴더에서 파일 수정 → GitHub 저장소에 다시 업로드하면 1~2분 내 반영

여러 심사 행사에 재사용할 수 있는 범용 심사 시스템입니다.

```
심사위원:  로그인(프로젝트 선택 + 이름 + 전화 뒤 4자리) → 점수·심사평 입력(실시간 자동 저장)
          → 심사완료 → QR 폰 서명 or 그 자리에서 서명 → 인쇄/PDF
관리자:    프로젝트 생성·공개/비공개 → 심사위원·평가항목·팀 명단 관리
          → 진행 현황·서명 관리 → 취합(합계/평균/순위) → 엑셀·PDF 저장
```

## 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 진입점 |
| `app.js` | 앱 전체 로직 |
| `style.css` | 화면 + A4 인쇄 스타일 |
| `config.js` | Supabase 키, 관리자 토큰, 최초 기본값 |
| `supabase.sql` | DB 스키마 (Supabase SQL Editor에서 1회 실행) |

## 1. 체험 모드 (지금 바로)

폴더에서 로컬 서버를 켜면 됩니다. 데이터는 그 브라우저에만 저장됩니다.

```bash
python3 -m http.server 8317 --directory ~/eduthon-judge
```

- 첫 화면: http://localhost:8317
- 관리자: http://localhost:8317/?admin=admin-7k2p9x

## 2. 실전 모드 (Supabase 연결)

여러 컴퓨터·휴대폰에서 같은 데이터를 보려면 필수입니다.

1. [supabase.com](https://supabase.com) → 새 프로젝트 생성 (무료)
2. **SQL Editor** → `supabase.sql` 내용 붙여넣기 → Run
3. **Settings → API** 에서 `Project URL`과 `anon public` 키 복사
4. `config.js`의 `SUPABASE_URL`, `SUPABASE_ANON_KEY`에 붙여넣기
5. `ADMIN_TOKEN`을 본인만 아는 값으로 변경

## 3. 배포 (폰 QR 서명을 쓰려면 인터넷 주소 필요)

정적 파일이라 아무 호스팅이나 됩니다. 추천: [Netlify](https://app.netlify.com/drop)에 폴더 드래그 → 즉시 URL 발급. (Vercel, GitHub Pages도 동일)

배포 후:
- 심사위원에게는 **기본 주소 하나만** 공유 (예: `https://내주소.netlify.app`)
- 관리자는 `https://내주소.netlify.app/?admin=관리자토큰`

## 4. 운영 순서

1. **관리자** → [＋ 새 프로젝트] (새 프로젝트는 **비공개**로 생성됨 — 미리 만들어두세요)
2. **프로젝트 설정**: 행사명·부문·심사일·팀 수 / 심사위원(이름·직책·**전화 뒤 4자리**) / 평가항목(항목·배점·세부기준) → [설정 저장]
3. **팀 명단** 입력 → [명단 저장]
4. 심사 당일: [⚫ 비공개] 버튼 → **공개** 전환
5. 심사위원들이 기본 주소에서 로그인 → 입력 (모든 입력은 실시간 자동 저장 — 브라우저가 꺼져도 다시 로그인하면 그대로)
6. [심사완료 · 서명] → 폰 QR 스캔 서명 or 그 자리 서명 → 심사표 잠김
7. 관리자: 현황판에서 입력 진행/서명 여부 확인, 취합에서 합계·평균·순위 실시간 확인
8. 종료 후: [📊 엑셀 저장](취합 + 위원별 시트 + 서명 이미지 포함, 편집 가능) / [🖨 취합 인쇄 · PDF]

## 5. 알아두기

- **인쇄/PDF**: 인쇄 버튼 → 대상에서 "PDF로 저장" 선택. 심사표는 A4 세로 1장에 맞게 최적화되어 있고, 심사평이 길어 넘치면 다음 장에 표 머리글이 반복됩니다.
- **입력 UX**: ↑↓←→ 화살표·Enter로 엑셀처럼 셀 이동, 점수는 배점 초과 시 자동 보정, 심사평은 Shift+Enter로 줄바꿈.
- **서명 후 수정**: 서명이 끝난 심사표는 잠기며, [수정하기] → 이중 경고를 거쳐야 풀립니다. 서명 후 수정된 심사표는 관리자 화면에 **⚠️ 서명 후 수정됨**으로 표시됩니다. 관리자는 서명을 [보기]/[삭제](재서명 요청)할 수 있습니다.
- **보안 수준**: 이름+전화 뒤 4자리는 간이 인증입니다. 행사 운영엔 충분하지만, anon 키가 클라이언트에 노출되는 구조이므로 민감한 심사(고액 지원금 등)라면 Supabase RLS 강화·서버 인증 도입을 권합니다.
- **전자서명 법적 효력**: 전자서명법 제3조에 따라 전자 형태라는 이유로 효력이 부인되지 않으며, 서명 화면에 동의 문구를 명시해 두었습니다. 서명 일시·기기 정보도 함께 저장됩니다. 상세는 대화 내 검토 내용 참고.
